To install packages:

Open a command prompt in the base folder "<your path>/python"

Run the command:
.\Scripts\pip install --user {package to install}

---------------------------------------------------------------
Run an app:

Open a command prompt in the base folder "<your path>/python"

Run the command:
python p/[project name]/app.py

**Read the readme file in the project folder first to setup necessary folder structure and input files**